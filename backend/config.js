export const PORT = 5555;

const name = 'alex';
const pass = '0000';
export const mongoDBURL = `mongodb+srv://${name}:${pass}@sandboxcluster.wh0wned.mongodb.net/?retryWrites=true&w=majority`;

// Please create a free database for yourself.
// This database will be deleted after tutorial